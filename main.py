import json
import sys
from assembler import Assembler

Assembler.run()

# fix this nik
#if '--run' in sys.argv:
#    import subprocess
#    subprocess.run([sys.executable, 'emulator.py', dump_fileName], shell=True)